import Logo from './Image assets/Logo.jpg';
function Header() {
    return (
        <header>
              <img src={Logo} alt="little lemon logo" />
        </header>
    );
}

export default Header;